

; That pytest will only test files in the tests directory and its subdirectories.
; That pytest will only run tests that are marked
; That pytest reports the reasons why tests have failed.  -rf

; -rsxX skipxfailXPass

; -l --tb=short --strict
; -m = marked


; 


[pytest]
testpaths = tests
markers =
yippee: Run the yippee test functions for slower plugin

addopts = -rf

